# AGENTS.md

This repository is brand new and currently empty. The project will be a modern frontend app (React/Vue/Next.js).

## Status

- No scaffold or package manager chosen yet.
- No build, test, lint, or format tooling installed.
- No README or CI configuration.

## Until the project is scaffolded

- Do not run package-manager or build commands expecting them to work; there is nothing to run.
- When scaffolding, pick the framework/toolchain, then update this file with the exact dev commands
  (dev server, build, lint, typecheck, test) and any repo-specific conventions.

## Once real code exists

Document here (and verify each item before adding):

- Exact dev/test/lint commands and any required order.
- Framework choice, entrypoints, and directory ownership.
- Any non-default conventions (styling, state management, testing setup).
